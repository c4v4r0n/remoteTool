/*
 * Transport layer placeholder. Concrete implementations
 * (TCP, TLS via OpenSSL/mbedTLS) land here in phase 2 behind the
 * rt_transport_ops_t vtable declared in include/network/transport.h.
 */

#include "network/transport.h"
